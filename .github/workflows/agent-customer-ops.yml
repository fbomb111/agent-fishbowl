name: Agent - Customer Ops

on:
  workflow_dispatch: {}   # Manual only — no email integration wired yet

permissions:
  contents: read
  issues: write

jobs:
  run:
    uses: ./.github/workflows/reusable-agent.yml
    with:
      role: customer-ops
    secrets: inherit
