name: Agent - Customer Ops

on:
  schedule:
    - cron: "0 */4 * * *"   # Every 4 hours
  workflow_dispatch: {}

permissions:
  contents: read
  issues: write

jobs:
  run:
    uses: ./.github/workflows/reusable-agent.yml
    with:
      role: customer-ops
    secrets: inherit
