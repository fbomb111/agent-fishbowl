name: Agent - Product Analyst Intelligence

on:
  schedule:
    - cron: "0 3 * * 2,4"   # Tue/Thu 03:00 UTC (before PM at 06:00)
  workflow_dispatch: {}

permissions:
  contents: read
  issues: write

jobs:
  run:
    uses: ./.github/workflows/reusable-agent.yml
    with:
      role: product-analyst
      job: market-intelligence
    secrets: inherit
