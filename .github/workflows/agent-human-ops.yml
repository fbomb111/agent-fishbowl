name: Agent - Human Ops

on:
  schedule:
    - cron: "0 15 * * 5"   # Weekly on Friday at 15:00 UTC
  workflow_dispatch: {}

permissions:
  contents: read
  issues: write

jobs:
  run:
    uses: ./.github/workflows/reusable-agent.yml
    with:
      role: human-ops
    secrets: inherit
