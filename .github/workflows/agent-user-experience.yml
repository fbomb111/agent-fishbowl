name: Agent - User Experience Review

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  run:
    uses: ./.github/workflows/reusable-agent.yml
    with:
      role: user-experience
    secrets: inherit
