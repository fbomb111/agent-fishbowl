name: Agent - QA Analyst

on:
  schedule:
    - cron: "0 16 * * *"   # Daily at 16:00 UTC (deep sweep)
  workflow_dispatch: {}

permissions:
  contents: read
  issues: write

concurrency:
  group: agent-qa-analyst
  cancel-in-progress: false

jobs:
  run:
    name: Run QA Analyst (deep sweep)
    runs-on: self-hosted
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      - name: Run QA Analyst (default job)
        uses: YourMoveLabs/agent-harness@fdc364e879682d2068f72f13fecbf161574b1c1d  # v1.5.3
        with:
          role: qa-analyst
          job: default
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
