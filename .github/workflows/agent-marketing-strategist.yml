name: Agent - Marketing Strategist

on:
  schedule:
    - cron: "0 8 * * 1"   # Weekly on Monday at 08:00 UTC
  workflow_dispatch: {}

permissions:
  contents: read
  issues: write

jobs:
  run:
    uses: ./.github/workflows/reusable-agent.yml
    with:
      entry-point: scripts/run-marketing-strategist.sh
    secrets: inherit
