name: Agent - Financial Analyst

on:
  schedule:
    - cron: "0 12 * * *"   # Daily 12:00 UTC
  workflow_dispatch: {}

permissions:
  contents: read
  issues: write

jobs:
  run:
    uses: ./.github/workflows/reusable-agent.yml
    with:
      entry-point: scripts/run-financial-analyst.sh
    secrets: inherit
